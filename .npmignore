# NOTE: npm 11+ ignores this file when `files` field is present in package.json.
# The `files` field in package.json is the authoritative whitelist for published content.
# This file remains as documentation and for compatibility with older npm versions.
#
# dist/ and glassbox/dist/ are pre-built and committed — never exclude them.
# See package.json `files` array for the full include list.

# Source TypeScript (compiled output is in dist/ — already in files whitelist)
src/
glassbox/src/
glassbox/node_modules/
glassbox/.gitignore
glassbox/tsconfig.json
glassbox/vite.config.*
glassbox/eslint.config.*
glassbox/index.html
tsconfig.json

# Exclude raw .ts source but keep compiled declaration files
*.ts
!*.d.ts
!*.d.ts.map

# Dev / CI config
.github/
.vscode/
.eslintrc*
eslint.config.*
vitest.config.*

# Misc
*.log
.DS_Store
node_modules/
