# Signal CLI Configuration for Cell 0 OS
# Copy this file to .env and customize for your setup

# ============================================
# REQUIRED SETTINGS
# ============================================

# Your Signal phone number with country code (e.g., +1234567890)
SIGNAL_CLI_PHONE_NUMBER=+1234567890

# ============================================
# OPTIONAL SETTINGS
# ============================================

# Port to expose signal-cli REST API
SIGNAL_CLI_PORT=8080

# Mode: 'normal' or 'native' (native for ARM64 performance)
SIGNAL_CLI_MODE=normal

# Enable native mode for better performance on ARM64 (Raspberry Pi, Apple Silicon)
SIGNAL_CLI_NATIVE_MODE=false

# Logging level: DEBUG, INFO, WARNING, ERROR
SIGNAL_CLI_LOG_LEVEL=INFO

# Path to existing signal-cli config (optional)
# Leave empty to use Docker volume
SIGNAL_CLI_CONFIG_PATH=

# ============================================
# ADVANCED SETTINGS
# ============================================

# Container resource limits
SIGNAL_CLI_CPU_LIMIT=2
SIGNAL_CLI_MEMORY_LIMIT=2G
SIGNAL_CLI_CPU_RESERVATION=0.5
SIGNAL_CLI_MEMORY_RESERVATION=512M

# ============================================
# CELL 0 OS INTEGRATION
# ============================================

# Cell 0 daemon WebSocket URL
CELL0D_WS_URL=ws://localhost:8765

# Enable MCIC (Multi-Channel Integration Controller) integration
MCIC_ENABLED=true

# Agent ID in MCIC
MCIC_AGENT_ID=signal_bot

# ============================================
# REGISTRATION (One-time setup)
# ============================================

# To register a new number:
# 1. Set SIGNAL_CLI_PHONE_NUMBER above
# 2. Run: docker-compose -f docker-compose.signal.yml run --rm signal-cli-register
# 3. Follow the prompts

# To verify with code received via SMS:
# curl -X POST http://localhost:8080/v1/register/+1234567890/verify \
#   -H "Content-Type: application/json" \
#   -d '{"verification_code": "123456"}'

# To link to existing device:
# 1. Run: docker-compose -f docker-compose.signal.yml run --rm signal-cli-register link
# 2. Scan the QR code with your Signal mobile app
