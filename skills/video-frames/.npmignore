# Intentionally empty — all files in this directory are included in the npm package.
# Without this file, npm falls back to .gitignore (which has Python/output patterns
# that could accidentally exclude future tracked files).
